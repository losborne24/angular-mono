// Shared Angular selector rules for every Angular project (app + lib).
// Spread this AFTER nx.configs['flat/angular'] so the @angular-eslint plugin
// is registered before these rules reference it.
export default [
  {
    files: ['**/*.ts'],
    rules: {
      '@angular-eslint/directive-selector': [
        'error',
        {
          type: 'attribute',
          prefix: 'app',
          style: 'camelCase',
        },
      ],
      '@angular-eslint/component-selector': [
        'error',
        {
          type: 'element',
          prefix: 'app',
          style: 'kebab-case',
        },
      ],
    },
  },
];
