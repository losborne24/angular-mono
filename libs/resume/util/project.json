{
  "name": "resume-util",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/resume/util/src",
  "projectType": "library",
  "tags": ["type:util", "scope:resume"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/resume/util"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
